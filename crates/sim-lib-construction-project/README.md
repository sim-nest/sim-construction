# sim-lib-construction-project

Construction project-control records for SIM.

The crate defines stable project and role identifiers, charter records, evidence
states, and deterministic readiness summaries. It stores references to source
systems through `sim-lib-doc-core` rather than copying external project content.
The embedded recipe uses modeled project data and describes a minimal accepted
charter.
